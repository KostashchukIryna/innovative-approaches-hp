## Explanation
The task was to fix code, so the most used word is `the`. It was done by normalization

Also replaced usage of arrays with Collections. `Map<String, Long>` for storing a word and its occurrence. And I used 
`stream()`.

